# 阶段一：默认配置（无需修改，当前即此状态）
# 直接使用现有 ~/.config/fsmon/fsmon.toml（全注释）

[monitored]
path = "~/.local/share/fsmon/monitored.jsonl"

[logging]
path = "~/.local/state/fsmon"

[socket]
path = "/tmp/fsmon-<UID>.sock"
