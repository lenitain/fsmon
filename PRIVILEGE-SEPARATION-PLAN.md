# fsmon 最小权限分离改造方案

> 状态：**规划稿**，未改动任何现有代码。
> 所有"实测"结论均可复现，探针源码见 `tools/verify-fanotify-privsep.c` 与本文附录。
> 环境：CachyOS / 内核 `7.2.4-3-cachyos-bore-lto` / fsmon 0.5.4。

---

## 0. 结论

**可以，而且 fsmon 能做到 gsr 做不到的一点：特权代码不必以可执行文件的形式存在。**

三条实测支撑：

1. **fsmon 的全部特权需求只是"创建 fanotify group"这一件事**（§1）。标记、读事件、
   FID→路径解析、进程追踪、写日志全都不需要特权 —— 实测 310 个事件、路径 100% 正确。
2. **特权属于 group 对象而非进程**，所以可以随 fd 传递（§3，已实测确认：
   `CapEff=0` 的读者拿到了 root 造成的事件的真实 pid）。
3. **group 必须按文件系统建** —— btrfs 子卷是弱 fsid，内核直接返回 `EXDEV`
   拒绝混合（§5.2 实测矩阵）。这条推翻了本文早期版本的判断，也正是它决定了最终架构。

于是最优架构是（§5.5）：

```
systemd(AmbientCapabilities=CAP_SYS_ADMIN + seccomp 白名单)
  └─ fsmon daemon  CapEff=0  ← 主进程零特权
       └─ fork 出的 "fanotify 工厂"  ← 唯一持有 CAP_SYS_ADMIN 的进程
            输入只有 (dirfd, flags, mask)，被 seccomp 限死在 8 个系统调用
```

**为什么这比 gsr 更好**：gsr 的 `gsr-kms-server` 必须是磁盘上带
`cap_sys_admin=ep` 的文件（录制会话由用户手动启动），而 file capability 是 inode 属性 ——
**谁能 exec 它，谁就在自己控制的进程里拿到不受任何过滤的 `CAP_SYS_ADMIN`**。
fsmon 是 systemd 服务，特权可以在 fork 时继承，于是特权代码**根本不存在于文件系统上**，
其他用户无法触达（§5.4）。

支点假设（§3）已于 `2026-09-16` 实测确认（§8）。

**依赖改动范围（自查后收敛）**：`fanotify-fid` **不需要任何改动**（§5.6.1）。
它自身另有若干**通用缺陷**值得独立修复（§5.6b），但那些与 fsmon 无关，
修不修都不影响本方案 —— 一个通用库不应为单个下游做特化。
全部改动都落在 fsmon 自己身上（§5.6.3）。

---

## 1. 实测：fsmon 的权限需求比它自己以为的小得多

`check_root()`（`src/common/monitor/init.rs:27`）要求 `euid == 0`。实测在 **uid=1000、CapEff=0**
下运行探针：

| 操作 | 需要特权？ | 实测结果 |
|---|---|---|
| `fanotify_init(CLASS_NOTIF\|REPORT_FID\|REPORT_DIR_FID\|REPORT_NAME)`<br>`init.rs:159` 的**原样 flag** | **否** | OK |
| `fanotify_mark(FAN_MARK_ADD, dirfd)` 标记 **root 拥有的** `/usr`、`/etc`、`/tmp` | **否** | OK |
| `fanotify_mark(FAN_MARK_MOUNT / FILESYSTEM)` | **是** `CAP_SYS_ADMIN` | EPERM |
| `FAN_UNLIMITED_QUEUE` / `FAN_UNLIMITED_MARKS` / `FAN_CLASS_CONTENT` | **是** `CAP_SYS_ADMIN` | EPERM |
| cn_proc 进程连接器（bind + listen + **实际收包**） | **否** | OK，fork 事件确实送达 |
| 读 `/proc/1/cmdline`、`/proc/1/status`（root 的进程） | **否** | OK |
| `name_to_handle_at` | **否** | OK |
| `open_by_handle_at` | **是** `CAP_DAC_READ_SEARCH` | EPERM |
| inode 标记 / 读事件 / 写日志 | **否** | OK |

注意 `mark(/root)` 失败是 `EACCES` 而非 `EPERM`——那是**路径穿越的 DAC 检查**，不是 capability。

### 端到端实测

把 `check_root()` 改成只告警，用 **uid=1000、CapEff=0** 真实跑一遍守护进程：

```
$ cat /tmp/np-home/.local/state/fsmon/_global_log.jsonl
{"event_type":"CREATE","path":"/tmp/np-watch/created-by-writer.txt",
 "pid":0,"comm":"","cmd":"","user":"lenitain","file_size":5,"ppid":0,"tgid":0,"chain":[]}
```

压力负载（嵌套 `mkdir -p`、跨目录 rename、delete、短命进程突发、mkdir 后立刻写入的竞态窗口）：

```
  总事件数        : 310
  路径正确解析    : 310   (100%)
  tier-3 回退调用 : 0     <-- open_by_handle_at 一次都没被需要
  pid 归因成功    : 0     <-- 全部归零
```

**路径解析、事件类型、文件大小、属主、时间戳全部正确。唯一坏掉的是 pid —— 而那正是 fsmon 存在的理由。**

---

## 2. 为什么 pid 会归零：内核的硬性规定

```c
/* fs/notify/fanotify/fanotify_user.c:675-683 (v6.12) */
metadata.pid = pid_vnr(event->pid);
/*
 * For an unprivileged listener, event->pid can be used to identify the
 * events generated by the listener process itself, without disclosing
 * the pids of other processes.
 */
if (FAN_GROUP_FLAG(group, FANOTIFY_UNPRIV) &&
    task_tgid(current) != event->pid)
        metadata.pid = 0;
```

这是一个**刻意的信息泄露防护**：非特权监听者只能看到自己造成的事件，别人的进程 pid 一律抹成 0。

`FANOTIFY_UNPRIV` 在 init 时按 `!capable(CAP_SYS_ADMIN)` 设置：

```c
/* fanotify_user.c:1411-1426 */
if (!capable(CAP_SYS_ADMIN)) {
        if ((flags & FANOTIFY_ADMIN_INIT_FLAGS) || !fid_mode)
                return -EPERM;
        internal_flags |= FANOTIFY_UNPRIV;
}
```

`capable()` 判定的是 **init_user_ns**，所以 **user namespace 救不了**——实测
`unshare -Ur`（命名空间内 CapEff 全满）依然是 `pid=0`。

**这排除了"干脆不要 root"这条路。** 归因能力必须靠真特权换来。

---

## 3. 关键洞察：特权属于 group 对象，可以随 fd 传递

这是整个方案的支点。内核把标志存在 **group** 上，而不是进程上：

```c
/* fanotify_user.c:1482,1497 */
group = fsnotify_alloc_group(&fanotify_fsnotify_ops, internal_flags, ...);
group->fanotify_data.flags = flags | internal_flags;   /* FANOTIFY_UNPRIV 在这里 */
```

而 §2 的 pid 闸门是**在 `read()` 时读 group 标志**：

```c
if (FAN_GROUP_FLAG(group, FANOTIFY_UNPRIV) && ...) metadata.pid = 0;
```

`read()` 本身**没有任何 capability 检查**。因此：

> 特权组件调用 `fanotify_init()` 创建 group → 得到普通 fd →
> 通过 `SCM_RIGHTS` 把 fd 交给非特权守护进程 → **group 依然是非 UNPRIV 的** →
> 守护进程读到真实 pid。

这与 gsr 把 `/dev/dri/card*` 的 fd 交给主进程是同一个手法。区别在于
gsr 的 helper 要一直活着供给帧，而 fsmon 的**读取侧**特权只需要出现一次。

> **但"一次就够"只对读取侧成立。** §5 会证明：group 必须按文件系统建，
> 而新文件系统随时可能出现，所以**创建侧**的特权必须常驻可用。
> 这正是最终架构里要有一个常驻工厂子进程的原因。

### ✅ 已实测确认（2026-09-16）

此假设已用 `tools/verify-fanotify-privsep.c` 在真实内核（`7.2.4-3-cachyos-bore-lto`）上跑通：

```
  [privileged component] uid=0    CapEff: 000001ffffffffff   ← 建 group、打标记
    group is privileged (FAN_UNLIMITED_MARKS accepted): YES
  [unprivileged reader (the daemon)] uid=1000 CapEff: 0000000000000000
    received fanotify fd 3                                   ← 仅靠 SCM_RIGHTS 获得
    expecting: root-writer pid=9381, unpriv-writer pid=9386
    reader sees: mask=0x100  pid=9381     <-- root-writer, CORRECT
    reader sees: mask=0x100  pid=9386     <-- unpriv-writer, CORRECT
=> CONFIRMED
```

关键点：读者的 `CapEff` 是 **全零**，却拿到了**由 root 造成的**事件的真实 pid。
同一个读者若自己调 `fanotify_init`（非特权路径），这个值会是 `0`。

### 一个佐证：fsmon 要的 flag 恰好就是"非特权可用集合"

```c
/* include/linux/fanotify.h */
#define FANOTIFY_USER_INIT_FLAGS  (FAN_CLASS_NOTIF | FANOTIFY_FID_BITS | \
                                   FAN_CLOEXEC | FAN_NONBLOCK)
#define FANOTIFY_ADMIN_INIT_FLAGS (FANOTIFY_PERM_CLASSES | FAN_REPORT_TID | \
                                   FAN_REPORT_PIDFD | FAN_UNLIMITED_QUEUE | \
                                   FAN_UNLIMITED_MARKS)
```

fsmon 在 `init.rs:159` 要的是
`FAN_CLOEXEC | FAN_NONBLOCK | FAN_CLASS_NOTIF | FAN_REPORT_FID | FAN_REPORT_DIR_FID | FAN_REPORT_NAME`
—— **一字不差就是 `FANOTIFY_USER_INIT_FLAGS`**。

也就是说：**特权给 fsmon 买到的只有一件事——清掉 `FANOTIFY_UNPRIV` 那一个 bit。**
没有任何别的功能依赖特权。

---

## 4. 其他"因为跑在 root 才存在"的东西

从代码看，除 fanotify 外唯一与 root 相关的只有 `chown`：

```
nix 调用全集: chown, dup, getegid, geteuid, open, statvfs
```

- `chown_to_user` / `chown_to_original_user`（`fid_parser.rs`、`clean/core.rs`、
  `monitor/file_writer.rs`、`monitored.rs`）——**纯粹是因为守护进程以 root 跑、
  产物是 root 属主才需要**。守护进程一旦以用户身份运行，这些调用自动变成无害 no-op。
- `resolve_uid_gid()`（`config.rs:270`）的分支逻辑（SUDO_UID → HOME 属主 → loginuid）
  同样只是"我是 root，我要猜真实用户是谁"的产物。
- `Config::user_path()` 里 `geteuid().is_root()` 时忽略 `XDG_CONFIG_HOME` 的
  防注入逻辑（F-006），在非 root 下自然走用户自己的配置，语义正确。
- `set_socket_permissions(0o666)`：socket 落在 `$XDG_RUNTIME_DIR`（`/run/user/1000`，
  权限 `0700`）内，实测其他用户不可达，所以不是漏洞，但 0666 没必要。

**没有** setuid / mount / ptrace / sched / ioprio / mknod 等其它特权调用。

---

## 5. 决定性约束：group 必须按文件系统拆分（内核强制）

> **本节推翻了本文早期版本的说法。** 早先我判断"一个 group 装所有文件系统"可行，
> 理由是一个 group 可以挂多个 mount_fd。**实测证明这是错的** —— 内核会直接拒绝。

### 5.1 内核规则

`fanotify_set_mark_fsid()`（`fanotify_user.c:1206-1223`）：

```c
/* Do not allow mixing of marks with weak and strong fsid */
if ((mark->flags ^ old->flags) & FSNOTIFY_MARK_FLAG_WEAK_FSID)
        return -EXDEV;

/* Allow mixing of marks with strong fsid from different fs */
if (!fsid->weak)
        return 0;                                  // 强 fsid：允许跨文件系统

/* Do not allow mixing marks with weak fsid from different fs */
if (old_sb != fsid->sb)
        return -EXDEV;                             // 弱 fsid：跨挂载点拒绝

/* Do not allow mixing marks from different btrfs sub-volumes */
if (!fanotify_fsid_equal(&FANOTIFY_MARK(old)->fsid, &FANOTIFY_MARK(mark)->fsid))
        return -EXDEV;                             // 弱 fsid：跨子卷拒绝
```

"弱 fsid"由 `fanotify_test_fsid()`（`:1565-1603`）判定：**dentry 的 fsid ≠ 它所在
`sb->s_root` 的 fsid** 即为弱。btrfs 的**所有子卷共用一个 super_block**，而
`sb->s_root` 是顶层子卷（subvolid 5）的根 —— 所以**任何非顶层子卷都是弱 fsid**。

### 5.2 实测矩阵

`/tmp/fscap/fsidmatrix.c` 对每一对路径新建 group、先标记 A 再标记 B：

```
  FIRST\2nd    /etc        /usr        /var/log    /home       /tmp        /dev/shm
  /etc         OK          OK          EXDEV       EXDEV       EXDEV       EXDEV
  /usr         OK          OK          EXDEV       EXDEV       EXDEV       EXDEV
  /var/log     EXDEV       EXDEV       OK          EXDEV       EXDEV       EXDEV
  /home        EXDEV       EXDEV       EXDEV       OK          EXDEV       EXDEV
  /tmp         EXDEV       EXDEV       EXDEV       EXDEV       OK          OK
  /dev/shm     EXDEV       EXDEV       EXDEV       EXDEV       OK          OK

  fsid:  /etc,/usr = 2442fa96624f73d5   /var/log = ...73d3   /home = ...73d4
         /tmp = 2e92b2352645752d        /dev/shm = ed95012b12b12bfa
```

结论：

| 观察 | 含义 |
|---|---|
| `/etc` + `/usr` OK | 同一 btrfs 子卷（`@`）→ 同 fsid → 可共享 |
| `/etc` + `/var/log` EXDEV | **不同 btrfs 子卷 → 各自的 group** |
| `/home` + 任何非 `@home` EXDEV | 同上 |
| `/tmp` + `/dev/shm` **OK** | 两者都是 **强 fsid** → 跨文件系统自由混合 |
| btrfs ↔ tmpfs 全部 EXDEV | 强/弱不能混在同一个 group |

**所以"一个 group 装下所有文件系统"在内核层面不可能。**
fsmon 现在按 `st_dev` 分组**不是偶然的实现细节，而是被内核强制的正确设计**。
我早先把它当成可以合并的冗余，是错的。

> **不要再"顺手优化"成按 fsid 分组。** 内核比较的是 `old_sb != fsid->sb` —— 是
> **superblock**，而 `st_dev` 正是它在用户态的视图。同一个 fsid 可以对应两个不同的
> superblock（同一子卷挂载两次），那时 fsid 分组会把内核会拒绝的两条路径放进同一个
> group。实测矩阵也印证：**所有同 `st_dev` 配对都被接受，所有 `EXDEV` 配对都不同 `st_dev`。**
> 详见 §5.6.2（那里同时撤回了我早先"`st_dev` 是潜在 bug"的错误说法）。

### 5.3 三个候选方案的判决

| 方案 | 判决 | 理由 |
|---|---|---|
| **(a) 合并为单一 group** | ❌ **不可行** | 上表实测：内核返回 `EXDEV`，不是设计取舍问题 |
| **(b) 启动时预建 group 池** | ❌ **无意义** | 池大小的上界不可知（用户随时可能挂载新文件系统）；且 group 有 **128/uid** 的硬上限（`max_user_groups`） |
| **(c) `setcap` helper 二进制** | ❌ **被严格劣于** | 见 §5.4 |

### 5.4 为什么 `setcap` helper 反而更差

gsr 的 `gsr-kms-server` 带 `cap_sys_admin=ep`。**file capability 是 inode 属性** ——
谁能执行这个文件，谁就在**自己控制的进程**里拿到 `CAP_SYS_ADMIN`：

- 若 helper 全局可执行 → 任何本地用户直接获得**无任何过滤**的 `CAP_SYS_ADMIN`；
- 若放进 `0700` 目录限制执行 → 那正是守护进程的 uid，等于该 uid 的任何进程
  （包括用户的 shell）都能拿到无过滤的 `CAP_SYS_ADMIN`。

对比之下，让**守护进程自己**持有 `CAP_SYS_ADMIN` 并叠加 seccomp 白名单：
这个能力只存在于 systemd 启动的那一个进程里，别的用户**根本拿不到**，
而且只能经由 `fanotify_init`/`fanotify_mark` 使用。

**结论：一个"能被别人 exec 的特权二进制"是比"被 seccomp 限死的特权进程"更大的洞。**
gsr 别无选择（它要访问 `/dev/dri`），fsmon 有。

### 5.5 最优解：常驻"fanotify 工厂"子进程

既然 group 必须按文件系统建、且新文件系统随时可能出现，特权就必须**常驻可用**。
最优做法不是在磁盘上放特权二进制，而是**在启动时 fork 一个只做一件事的子进程**：

```
systemd unit
  User=lenitain
  AmbientCapabilities=CAP_SYS_ADMIN          # 只为让工厂子进程继承
  CapabilityBoundingSet=CAP_SYS_ADMIN
  NoNewPrivileges=yes
  SystemCallFilter=@system-service fanotify_init fanotify_mark
        │
        ▼
  fsmon daemon (uid 1000)
   ├─ 启动时 fork() ──► [fanotify 工厂]  ← 继承 CAP_SYS_ADMIN
   │                      • seccomp 白名单：fanotify_init, fanotify_mark,
   │                        recvmsg, sendmsg, read, write, close, exit_group
   │                      • 循环：收 {dirfd, flags, mask} →
   │                        fanotify_init + fanotify_mark(fd, ADD, mask, dirfd, ".")
   │                        → 用 SCM_RIGHTS 回传 fan_fd
   │                      • 永不接触路径字符串、JSON、事件流
   │
   └─ 主进程：capset(0,0,0) 之后 CapEff 全零
              • 按 fsid 向工厂要 group，持有并读取事件，完整归因
```

这个设计的关键性质：

| 性质 | 说明 |
|---|---|
| 主进程 `CapEff=0` | 与 gsr 主进程同样的可审计性质 |
| 特权代码面极小 | 输入只有 `(dirfd, flags, mask)` —— **没有字符串、没有路径解析、没有 JSON** |
| 不可被其他用户触达 | 不是磁盘上的特权文件，是 fork 出来的子进程；socketpair 的另一端只有父进程 |
| 无混淆代理 | 子进程不解析任何用户输入；路径由父进程以 **已打开的 dirfd** 形式传入 |
| 覆盖完整 | 任何时刻出现的新文件系统都能建 group，无需重启、无上限 |
| 失败模式清晰 | 工厂返回 `EXDEV`，主进程据此为该 fsid 另建 group |

**与 gsr 的对照**：同样是"极小特权组件"，但 gsr 的特权组件必须放在磁盘上并授予
file capability（因为录制时会话由用户手动启动）；fsmon 是 systemd 服务，
特权可以在 fork 时继承 —— 于是**特权代码根本不以可执行文件的形式存在**。

### 5.6 需要改动的只有 fsmon 自己

> **本节经过一次自我纠正。** 早先版本要求改 `fanotify-fid`（"解析出 fsid，让 fsmon 按
> fsid 分组"）。重新推导后：**那是 fsmon 驱动的特化，fsmon 根本不需要它。**
> 理由见 §5.6.1。`fanotify-fid` 是发布在 crates.io 上的通用库，它的改动必须
> 独立成立（§5.6.2 给出了确实成立的那些）。

#### 5.6.1 为什么 fsmon 不需要动库

1. **分组发生在"打标记"时，那时还没有任何事件。**
   fsmon 要判断"这个路径该放进哪个 group"，而 fsid 完全可以在用户态自己算：
   `statfs(path).f_fsid` 就是内核 `vfs_get_fsid()` 的取值来源。
   事件里的 fsid 是**事后**才有的，用不上。
   （实测已确认：`fsidmatrix` 打印的 f_fsid 与内核的接受/拒绝判定完全吻合。）
2. **一个 group 一个文件系统时，库的线性扫描本来就是 O(1)。**
   `handle.rs:139` 的 `resolve_file_handle` 遍历 `mount_fds`；
   fsmon 每个 reader 只传**一个** mount fd（`reader.rs:111`），
   遍历长度恒为 1。所谓 O(F) 问题在按文件系统分组的前提下不存在。

所以：**fsmon 侧的零库改动方案是完整的。**

#### 5.6.2 顺带纠正：`st_dev` 是对的键，不要改成 fsid

早先版本说"内核真正的键是 fsid，fsmon 用 `st_dev` 是潜在 bug"。
**这个说法应当撤回**，它说反了。内核的弱 fsid 判定是：

```c
if (old_sb != fsid->sb) return -EXDEV;   /* 比较的是 superblock */
```

`st_dev` 正是 superblock 在用户态的视图。而**单纯按 fsid 分组反而更不安全**：
同一个 fsid 可以对应两个不同的 superblock（例如同一子卷被挂载两次），
那时内核会因为 `old_sb != fsid->sb` 拒绝，而 fsid 分组会把它们放进同一个 group。
实测矩阵也支持这一点：**所有同 `st_dev` 的配对都被接受，所有 `EXDEV` 配对都不同 `st_dev`。**

**结论：`st_dev` 保持不变。** 之前那条"潜在静默 bug"是我的臆测，不成立。

#### 5.6.3 fsmon 的实际改动清单

1. `init.rs` / `live_path.rs` / `temp_marks.rs` **三处 `fanotify_init` 全部删掉**，
   统一改成向工厂请求 group。
2. `init.rs:27` 的 `check_root()` → 能力探测 + 非特权时响亮报错（阶段 1）。
3. 启动流程：fork 工厂 → 建初始 group 与标记 → `capset(0,0,0)` → 进入事件循环。
4. **不授予 `CAP_DAC_READ_SEARCH`**；`mount_fds` 传空 slice，
   让 tier-3 回退零开销地失败并计入计数器（§6 阶段 4）。
5. 用 `FAN_UNLIMITED_MARKS` 建组 —— 见 §5.7。
6. **分组键保持 `st_dev`**（§5.6.2）。

**`fanotify-fid` 的改动：0 行。**

---

### 5.6b `fanotify-fid` 自身的通用缺陷（与 fsmon 无关，独立成立）

按"这个库对**所有**使用者是否更优"重新审查了一遍，确实发现若干**通用**问题。
它们不是 fsmon 的需求，也不为 fsmon 服务；修它们是因为它们本身就是缺陷。
**修与不修都不影响本方案的实施。**

#### 问题：7 种 info type 只解析了 3 种，且丢失是静默的

内核 `include/uapi/linux/fanotify.h:140-150` 定义了 7 种：

| info_type | 名称 | crate 是否定义常量 | crate 是否解析 |
|---|---|---|---|
| 1 | `FID` | ✅ | ✅ |
| 2 | `DFID_NAME` | ✅ | ✅ |
| 3 | `DFID` | ✅ | ✅ |
| **4** | **`PIDFD`** | ❌ | ❌ |
| **5** | **`ERROR`** | ❌ | ❌ |
| **10** | **`OLD_DFID_NAME`** | ❌ | ❌ |
| **12** | **`NEW_DFID_NAME`** | ❌ | ❌ |

`parse.rs:118` 是 `_ => {}` —— **静默丢弃，调用方拿不到任何"数据被丢了"的信号**。

#### 实测证据（`tools/fidtest/`，用 crate 自己的公开 API）

针对 **crates.io 上发布的 `fanotify-fid` 0.7.0**（`Cargo.lock` 锁定版本，不依赖本地检出）：

```
── CONTROL: create (type 2 DFID_NAME) ──
    mask = 0x100 ["CREATE"]     dfid_name = Some("control.txt")     ← 正常

── RENAME (types 10 + 12) ──
    mask = 0x10000000 ["RENAME"]  dfid_name = None                  ← 新旧文件名全丢

── FAN_REPORT_PIDFD (type 4) ──
    mask = 0x100 ["CREATE"]     dfid_name = Some("x.txt")           ← pidfd 丢了

── FAN_FS_ERROR (type 5) ──
    mask = 0x8000 ["FS_ERROR"]  dfid_name = None                    ← error/error_count 全丢

── UNKNOWN future type (99) ──
    mask = 0x100 ["CREATE"]     dfid_name = None                    ← 无任何提示
```

#### 为什么这是通用缺陷而不是"用不到的功能"

crate **自己声明并导出了**这些能力，解析器却交付不了：

- `consts.rs:145` 导出 `FAN_RENAME`，`:273` 把它映射成字符串 `"RENAME"` ——
  而 `FAN_RENAME` 的信息**全部**承载在 type 10/12 里。**结果是 RENAME 事件解析出来
  只有一个 mask，没有路径、没有名字、没有任何句柄。**
- `builder.rs:100` 暴露 `report_pidfd()` —— type 4 记录的**全部意义**就是那个 race-free 的
  pidfd，解析器把它丢掉后这个 flag 等于没开。
- `builder.rs:108` 暴露 `report_target_fid()` —— 与之配套的 rename 记录同样未被解析。

**一个库不应暴露它交付不了的开关**，更不该在丢失数据时不告知调用方。

#### 建议的改法（只增不减，不牺牲任何现有能力）

1. **补齐常量**：`FAN_EVENT_INFO_TYPE_PIDFD/ERROR/OLD_DFID_NAME/NEW_DFID_NAME`。
2. **补齐解析**：
   - `PIDFD` → `FidEvent` 上暴露 `pidfd: Option<RawFd>`（或 `OwnedFd`，由调用方决定）。
   - `ERROR` → 暴露 `error: Option<(i32, u32)>`。
   - `OLD_DFID_NAME` / `NEW_DFID_NAME` → 暴露 rename 的源/目标 `(handle, name)`。
3. **给未识别的 info type 一个出口**：例如 `FidEvent::unknown_info_types() -> &[u8]`
   或在 `debug` 下记一条日志。**让"数据被丢弃"这件事可被观测。**
4. **可选（纯增量）**：把 `fsid` 也暴露出来。
   对**同时监控多个文件系统的调用方**是有价值的通用能力 ——
   用单个 group 覆盖多个强 fsid 文件系统时，可据此把 `mount_fds` 从线性扫描变成路由。
   **但这必须在保留现有 `&[OwnedFd]` 线性扫描行为的前提下叠加**，
   不能替换掉它，否则会削弱现有能力（调用方可能依赖"挨个试直到成功"的语义）。
5. **文档纠错**：`README.md` 与 `lib.rs:17` 都写着需要 `CAP_SYS_ADMIN`。
   对 `FAN_CLASS_NOTIF` + FID 模式这是**错的** —— 实测非特权用户可用
   （`tools/captest2.c`：`CLASS_NOTIF|REPORT_FID|REPORT_DIR_FID|REPORT_NAME` 全 OK）。
   真正需要特权的是 `FAN_MARK_FILESYSTEM`/`FAN_MARK_MOUNT`（README 的示例恰好用了它，
   所以这句话被以偏概全了）。这直接误导使用者去要 root。

这些都**只增加**能力，不改变现有的 1/2/3 三种类型的解析行为，也不改变任何函数签名语义
（新增字段对 0.x 版本是允许的破坏性变更，需同步 `CHANGELOG` 与小版本号）。

### 5.7 顺带必需：`FAN_UNLIMITED_MARKS`

`fanotify_add_new_mark`（`:1243`）把 mark 计入 **per-uid ucount**：

```c
if (!FAN_GROUP_FLAG(group, FAN_UNLIMITED_MARKS) &&
    !inc_ucount(ucounts->ns, ucounts->uid, UCOUNT_FANOTIFY_MARKS))
        return ERR_PTR(-ENOSPC);
```

实测本机 `max_user_marks = 143129`，且**这个限制对 root 同样生效**——
计量路径里没有任何 `capable()` 旁路，唯一的豁免就是组上的 `FAN_UNLIMITED_MARKS`。

fsmon 用 `mark_recursive_with_depth` 递归打标，监控一棵大树上限就是 143129 个目录。
**启动时那一次特权是唯一能拿到豁免的时机**，所以建组时必须带上
`FAN_UNLIMITED_MARKS`（`builder.rs` 已有 `unlimited_marks()`）。

> 不要带 `FAN_UNLIMITED_QUEUE`：它把内核侧背压彻底移除，
> 把"极端负载下丢事件"换成"内核内存无限增长直到 OOM"。
> 现有 `max_queued_events=16384` 是有价值的最后防线，保留它。

---

## 6. 分阶段方案

### 阶段 1 —— 让权限需求变诚实（不改权限模型，可独立发布）

当前最危险的不是权限大，而是**失败是静默的**：一个非特权启动的 fsmon 会正常运行、
正常记录路径，然后给每一条事件写上 `pid: 0`。对一个以归因为卖点的工具，这是最坏的结果。

1. **把 `check_root()` 换成能力探测。** 判定"我是否特权"最忠实的方法是问内核一个
   只属于 `FANOTIFY_ADMIN_INIT_FLAGS` 的 flag：

   ```rust
   /// 内核用 capable(CAP_SYS_ADMIN) 决定是否给 group 打上 FANOTIFY_UNPRIV。
   /// 直接问一个管理专用 flag 最忠实——它比 capget() 更准（userns 里 capget 会说谎）。
   fn has_privileged_fanotify() -> bool {
       fanotify_init(FAN_CLOEXEC | FAN_CLASS_NOTIF | FAN_REPORT_FID
                     | libc::FAN_UNLIMITED_MARKS, O_RDONLY | O_CLOEXEC).is_ok()
   }
   ```

   失败时报错要说清后果，而不是说"请用 sudo"：

   ```
   fsmon 需要 CAP_SYS_ADMIN 才能创建特权 fanotify group。
   缺少它时内核会把「其他进程造成的」事件的 pid 抹成 0
   （fanotify_user.c: metadata.pid = 0），进程归因会静默失效。
   ```

2. **在 `init_fanotify()` 后再校验一次**，若非特权则打印一次醒目警告并计入 metrics。
3. 修 `help.rs` 里 `requires sudo for fanotify` 等措辞。

> 阶段 1 是**唯一一个无论后面怎么走都该做的改动**。它把一个静默的正确性陷阱
> 变成一个响亮的启动错误。

---

### 阶段 2 —— systemd 能力收敛（配置为主，投入产出比最高）

fsmon 的部署形态本来就是 system 服务（`/etc/systemd/system/fsmon.service`），
所以**不需要 file capability、不需要新二进制、不需要 IPC**。

现状：`User=` 未设置 → root 跑，拥有 **41 个 capability**（含 `CAP_SYS_MODULE`、
`CAP_DAC_OVERRIDE`、`CAP_SETUID`、`CAP_NET_ADMIN`…）和整个文件系统的写权限。

```ini
[Service]
Type=notify
User=lenitain
ExecStart=/usr/local/bin/fsmon daemon

# fanotify_init() 唯一需要的能力：用来清掉 group 的 FANOTIFY_UNPRIV 标志
AmbientCapabilities=CAP_SYS_ADMIN
CapabilityBoundingSet=CAP_SYS_ADMIN
NoNewPrivileges=yes

Restart=always
RestartSec=5
RestartPreventExitStatus=2
StartLimitBurst=5
StartLimitIntervalSec=300

# fsmon 只写自己的 store 和日志
ProtectSystem=strict
ProtectHome=read-only
ReadWritePaths=/home/lenitain/.local/share/fsmon /home/lenitain/.local/state/fsmon
ProtectKernelTunables=yes
ProtectKernelModules=yes
ProtectControlGroups=yes
RestrictNamespaces=yes
RestrictRealtime=yes
LockPersonality=yes
MemoryDenyWriteExecute=yes

# 注意：见下方"坑"
SystemCallFilter=@system-service fanotify_init fanotify_mark
SystemCallArchitectures=native

[Install]
WantedBy=multi-user.target
```

**坑（实测）**：`fanotify_init` / `fanotify_mark` 属于 systemd 的 **`@privileged`** 组，
**不在 `@system-service` 里**（`systemd-analyze syscall-filter @system-service | grep -c fanotify` → `0`）。
所以 `SystemCallFilter=@system-service` 会直接把 fanotify 掐掉，
必须像上面那样**显式追加这两个系统调用**。同理，反选式的 `~@privileged` 也会误伤。

**不是坑，是收获（实测）**：`@system-service` 里
`mount / umount2 / setns / unshare / bpf / perf_event_open / init_module / kexec_load /
reboot / swapon / pivot_root / chroot / quotactl / open_by_handle_at / add_key / keyctl /
ptrace` **全部 absent**。也就是说 `CAP_SYS_ADMIN` 的所有经典滥用出口都被 seccomp 堵死了，
剩下的唯一用途就是 `fanotify_init`。这正是阶段 3 敢于让工厂子进程持有该能力的前提
（`open_by_handle_at` 也不在里面 —— 阶段 4 的结论由此自动成立）。

效果：uid `0 → 1000`；capability `41 → 1`；文件系统从"全盘可写"收窄到两个目录。

**需要配套的代码改动**（都很小）：
- 放宽 `check_root()`（阶段 1 已完成）。
- `User=lenitain` 后 `HOME` 由 systemd 设置，可删掉现在硬编码的
  `Environment=HOME=/home/lenitain`。
- **一次性迁移**：此前 root 跑出来的 `~/.local/share/fsmon/`、`~/.local/state/fsmon/`
  里的文件是 root 属主，需要 `sudo chown -R lenitain:lenitain` 一次，否则
  `fsmon clean` / `fsmon query` 会失败。

> 另一个坑：`PrivateTmp=yes` 会让守护进程看到**私有 /tmp**，
> 如果监控目标在 `/tmp` 下必须去掉这一行。

---

### 阶段 3 —— fanotify 工厂 + 主进程降权（真正的 gsr 性质）

阶段 2 之后 `CAP_SYS_ADMIN` 长期驻留在**整个守护进程**里，而它只在一个地方被需要。
原本的设计是"建完 group 就丢掉全部能力"，但 §5 证明**这条路走不通**：
group 必须按文件系统建，而新文件系统随时可能出现，所以特权必须**常驻可用**。

于是最优解是**把特权关进一个只做一件事的子进程**（§5.5）：

```rust
/// 启动时 fork 出的 "fanotify 工厂"。
///
/// 它继承 CAP_SYS_ADMIN，随后把自己锁进一个极小的 seccomp 白名单；
/// 它的全部输入只有 (dirfd, flags, mask) —— 没有路径字符串、没有 JSON、
/// 没有事件流。工作就是 fanotify_init + fanotify_mark + SCM_RIGHTS 回传 fd。
///
/// 为什么不是 setcap 二进制：file capability 是 inode 属性，谁能 exec 谁就
/// 在自己控制的进程里拿到无过滤的 CAP_SYS_ADMIN。fork 出来的子进程则
/// 根本不以可执行文件形式存在，其他用户无法触达。
fn spawn_fanotify_factory() -> Result<FactoryHandle>;

/// 主进程：工厂就绪后立刻丢掉自己的一切特权。
/// 内核把 FANOTIFY_UNPRIV 存在 group 对象上而非进程上（§3），
/// 所以这里返回之后，那些 group 依然报告真实 pid。
pub(crate) fn drop_privileges() -> Result<()> {
    nix::unistd::setgroups(&[])?;                        // 先组
    nix::unistd::setgid(Gid::from_raw(gid))?;
    nix::unistd::setuid(Uid::from_raw(uid))?;
    debug_assert!(nix::unistd::setuid(Uid::from_raw(0)).is_err());  // 回不去
    clear_all_capabilities()?;   // capset(0,0,0) + 逐位 PR_CAPBSET_DROP
    prctl(PR_SET_NO_NEW_PRIVS, 1)?;
    Ok(())
}
```

调用顺序（`Monitor::run()` 内）：

```
fork 工厂 → 工厂装 seccomp 白名单 → 建初始 group 与标记
          → 主进程 capset(0,0,0) → 进入事件循环
```

| | 阶段 2 | 阶段 3 |
|---|---|---|
| 主进程 uid | 1000 | 1000 |
| 主进程长期持有的 capability | `CAP_SYS_ADMIN` | **0** |
| 特权驻留位置 | 整个守护进程 | 一个只认 `(dirfd,flags,mask)` 的子进程 |
| 其他用户能否取得该特权 | 否 | **否**（磁盘上不存在特权文件） |

工厂子进程的 seccomp 白名单只需：
`fanotify_init, fanotify_mark, recvmsg, sendmsg, read, write, close, exit_group`。
配合 unit 上的 `SystemCallFilter=@system-service fanotify_init fanotify_mark`，
两条防线都指向同一个极小集合。

> **注意**：unit 的 `SystemCallFilter` 会**被子进程继承且不可撤销**，
> 所以 `fanotify_init`/`fanotify_mark` 必须显式写进白名单 ——
> 它们属于 systemd 的 `@privileged` 组，**不在 `@system-service` 里**（实测 `grep -c` → 0）。

---

### 阶段 4 —— `CAP_DAC_READ_SEARCH`：不授予，让它零开销地失败

`open_by_handle_at` 需要它，而且**即使对调用者自己拥有的文件也需要**（实测：
`name_to_handle_at` OK，`open_by_handle_at` 同一个目录 EPERM）。

但实测显示它在实践中**根本不被调用**：310 个事件、0 次 tier-3 回退。
原因是 fsmon 的设计已经用两级缓存绕开了它——
`cache_recursive()`（`init.rs:221`）在启动时用 **`name_to_handle_at`（无需特权）**
预热全部目录句柄，新增目录在打标记时同步入缓存。

失败模式也是良性的：`resolve_file_handle()`（fanotify-fid `handle.rs:158`）返回
`Option<PathBuf>`，失败只是 None —— **丢的是单个事件的路径，不是整批**。

**决定：不授予。** 而且这条路在阶段 3 之后**本来就是死的** ——
`open_by_handle_at` 不在 systemd 的 `@system-service` 白名单里（实测：`absent`），
即使把 capability 放进 bounding set 也用不出来。

**做法**：主进程向库传 `mount_fds = &[]`。`resolve_file_handle` 对空 slice
直接返回 `None`，于是**每次回退都是零系统调用的立即失败**，
而不是"拿着一个永远 EPERM 的 fd 列表逐个试"。同时在
`DirCacheStore::get` 未命中时累加 `metrics` 计数器并在 `fsmon health` 里暴露，
让"路径解析降级"从静默变成**可观测、可测量**的指标。

> **绝对不要把 `CAP_DAC_READ_SEARCH` 放进那个特权子进程。** 那会造出一个真正的
> **文件读取 oracle**：`name_to_handle_at` 不需要任何权限，所以任何能向它发请求的
> 调用方都能用它读 `/etc/shadow`。而 `CAP_SYS_ADMIN` 只够建 fanotify 组，
> 滥用面几乎为零（§5.4 / §7）—— **两者不可相提并论**。

---

### 阶段 5 —— 打包与文档

- 走阶段 2/3 的话，**不需要 `setcap`，也就没有 gsr 那种"源码安装要手动授权一次"的问题**。
  `AmbientCapabilities` 由 systemd 在 fork 时施加，不落盘、不可被本地用户复用。
- `fsmon init --service` 应当直接生成**加固版** unit，而不是现在的裸版本。
- README 的 `requires root for fanotify` 需要改写为"需要 `CAP_SYS_ADMIN` 创建特权
  fanotify group"，并解释缺省时归因会失效。
- 若最终需要 helper 二进制（阶段 5c），在 unit 里用
  `ExecStartPre=setcap cap_sys_admin+ep /usr/local/libexec/fsmon-fanotify-helper`，
  并在 `%post`/README 里说明——这正是 gsr 走过的路。

---

## 7. 与 gsr 的对照，以及 confused deputy 分析

| | gsr | fsmon（阶段 3 之后） |
|---|---|---|
| 特权组件 | `gsr-kms-server` | fork 出的 "fanotify 工厂" |
| 特权组件的存在形式 | **磁盘上的文件** + `cap_sys_admin=ep` | **只是一个子进程**，不在文件系统上 |
| 谁能取得该特权 | 任何能 exec 该文件的用户 | 只有守护进程（fork 继承） |
| 特权进程的生命周期 | 整个录制会话 | 整个守护进程生命周期（但代码面极小） |
| 特权代码的输入面 | argv / 环境变量 / socket / 路径 | `(dirfd, flags, mask)` —— 无字符串 |
| 主进程 | `CapEff=0` | `CapEff=0` |
| 传递的资源 | `/dev/dri/card*` fd | fanotify group fd |
| 需要手动授权？ | 源码安装时需要一次 `setcap` | **不需要**（systemd 在 exec 时施加） |

**为什么 fsmon 能做到 gsr 做不到的**：gsr 的录制会话由用户手动启动，没有 supervisor
可以代为施加能力，所以 `gsr-kms-server` 只能靠 file capability 存在磁盘上。
fsmon 是 systemd 服务，`AmbientCapabilities` 在 fork/exec 时由 systemd 注入，
于是特权可以只活在进程里。

**file capability 的本质问题**（这是 §5.4 判决 (c) 的依据）：

> file capability 是 **inode 属性**，不是"给这个程序的"能力，而是
> **"给任何执行这个 inode 的进程的"能力**。谁能 exec，谁就在**自己控制的进程**里
> 拿到完整、无过滤的 `CAP_SYS_ADMIN` —— 可以注入自己的环境变量、自己关闭 seccomp、
> 自己决定怎么用。

一个被 seccomp 限死在 `fanotify_init`/`fanotify_mark` 上、且**别人根本 exec 不到**的
子进程，严格优于一个"任何同 uid 进程都能 exec 并拿到无过滤特权"的磁盘文件。

**两类 capability 的委托性质完全不同**：

- **`CAP_SYS_ADMIN`（仅用于建 fanotify 组）**：滥用面**几乎为零**。
  最坏结果是"创建一个 fanotify 监听组 + 给自己发 pid" —— 而**无 pid 的**监听
  本来就是内核允许任何非特权用户做的事。唯一多出来的是 pid 归因，
  且只能针对调用方自己打得开（能穿越路径）的目录。对比 `gsr-kms-server`
  能被用来**截屏**，风险等级低得多。
- **`CAP_DAC_READ_SEARCH`（解析句柄）**：**是真正的漏洞**。
  `name_to_handle_at` 不需要任何权限，所以任何能提出请求的一方
  都能把它当**任意文件读取 oracle**（读 `/etc/shadow`）。

**所以：`CAP_SYS_ADMIN` 可以委托，`CAP_DAC_READ_SEARCH` 不行。**
后者应该被"消除需求"（阶段 4），而不是"委托"。

---

## 8. 支撑假设的验证 ✅ 已完成

整个方案的支点（§3：特权随 fd 传递）原先只是**读内核源码得出的推断**。
已于 `2026-09-16` 用 `tools/verify-fanotify-privsep.c` 在真实内核
（`7.2.4-3-cachyos-bore-lto`）上实测确认。

### 探针做什么

1. 特权组件（uid=0）建 group + 打标记，并先用 `FAN_UNLIMITED_MARKS` 自证这确实是**特权组**；
2. fork 出非特权读者，**让它先 `close()` 掉 fork 继承来的 fd** —— 否则测试毫无意义；
3. 通过 `SCM_RIGHTS` 把 fd 交给读者；
4. 再造两个事件：一个由 **root** 造成，一个由 **uid=1000** 造成；
5. 检查读者拿到的 `pid` 是否等于这两个造事者的真实 pid。

### 实测输出

```
target unprivileged uid=1000 gid=1000
  [privileged component (this process)] uid=0 euid=0 CapEff: 000001ffffffffff

[1] privileged group created on /tmp/fanotify-privsep-test (fd 3)
    group is privileged (FAN_UNLIMITED_MARKS accepted): YES
  [unprivileged reader (the daemon)] uid=1000 euid=1000 CapEff: 0000000000000000
    received fanotify fd 3
    expecting: root-writer pid=9381, unpriv-writer pid=9386
    reader sees: mask=0x100  pid=9381     <-- root-writer, CORRECT
    reader sees: mask=0x100  pid=9386     <-- unpriv-writer, CORRECT
--- harness self-check ---
  unprivileged writer exit status : 0  (file created OK)
=== RESULT ===
CONFIRMED
```

| 检查项 | 结果 |
|---|---|
| 非特权读者能否只靠传入的 fd 读到事件 | ✅ |
| 能否看到 **root**（权限高于自己）造成的事件的真实 pid | ✅ `pid=9381` |
| 能否看到 **同 uid 另一进程**造成的事件的真实 pid | ✅ `pid=9386` |
| 读者自身权限 | `uid=1000 CapEff=0000000000000000` |

**最有说服力的一点**：读者的 `CapEff` 是**全零**，却拿到了**由 root 造成的**事件的真实 pid。
同一个读者若自己去调 `fanotify_init()`（走非特权路径），这个值必然是 `0`。

**结论：阶段 3 可行。** fsmon 只需在启动时持有 `CAP_SYS_ADMIN` 完成 `fanotify_init()`，
之后可以丢弃全部能力，归因能力不受影响。

### 复现

```bash
cd ~/.projects/fsmon/tools
gcc -O2 -o verify-fanotify-privsep verify-fanotify-privsep.c
sudo ./verify-fanotify-privsep 1000
```

若输出 `REFUTED`，则特权组件必须自己读事件流，退化为常驻 broker（gsr 原教旨形态）。

> **为什么探针里有一段 harness 自检。** 第一版把测试目录建成 `root:root 0755`，
> 非特权写者 `fopen` **静默失败** → 一个事件都没产生 → 读者空等 40 秒后报出
> **假阴性 `REFUTED`**。这个假阴性足以让人错误地否掉整个阶段 3。
> 自检会单独打印"非特权写者是否真的写出了文件"，**任何结论都必须先通过它**。

---

## 9. 未验证项与风险

| 项 | 状态 |
|---|---|
| 特权随 fd 传递 | ✅ **已实测确认**（§8，`CapEff=0` 的读者拿到 root 事件的真实 pid） |
| "合并为单一 group" | ❌ **已实测否证**（§5.2，内核返回 `EXDEV`，btrfs 子卷是弱 fsid） |
| "一个 group 一个文件系统"是被内核强制的 | ✅ **已实测确认**（§5.2 全矩阵） |
| `@system-service` 能挡住 `CAP_SYS_ADMIN` 的滥用路径 | ✅ 已实测：`mount/setns/unshare/bpf/perf_event_open/init_module/kexec_load/reboot/swapon/pivot_root/chroot/quotactl/open_by_handle_at/add_key/keyctl/ptrace` **全部 absent** |
| 阶段 2 的 unit 加固项 | 未实跑；`SystemCallFilter` 的坑已实测确认（`fanotify_*` 不在 `@system-service`，必须显式追加） |
| **fanotify 工厂子进程** | 未实现。seccomp 白名单需要覆盖父子通信所需的全部系统调用，须实跑调通 |
| Intel / 非 Btrfs / NFS 等文件系统 | 未测。**NFS/FUSE 可能是弱 fsid 或根本不能编码 FID** → 需要 §5.2 那样的矩阵对目标机器先测一遍 |
| `st_dev` → `fsid` 改键 | ❌ **已撤回**：内核比较的是 superblock，`st_dev` 才是对的键（§5.6.2） |
| `fanotify-fid` 需要为 fsmon 改动 | ❌ **已撤回**：0 行（§5.6.1）。它自身的通用缺陷见 §5.6b，与本方案解耦 |
| `fanotify-fid` 丢弃 4/5/10/12 号 info type | ✅ **已实测确认**（§5.6b，`tools/fidtest/`，RENAME 事件解析后无路径无名字） |
| `ProtectHome=read-only` 是否会妨碍监控 `/home` | 理论上不妨碍（只读取元数据 + 打 inode 标记），未实测 |

**采纳顺序**：
**阶段 1** 是纯收益、无风险的独立改动，先做。
**阶段 2 + 阶段 3** 必须一起做 —— 单独做阶段 3 没有意义（工厂仍需要
`AmbientCapabilities` 才有 `CAP_SYS_ADMIN` 可继承），单独做阶段 2 则 `CAP_SYS_ADMIN`
仍然驻留在整个守护进程里。
**阶段 4** 是阶段 3 的必然推论（`open_by_handle_at` 已被 seccomp 挡死）。

---

## 附录：复现本文所有实测

```bash
# 1) 能力探针（无需 root）
gcc -O0 -o captest2 captest2.c && ./captest2      # §1 表格
./captest4                                        # §2  pid 归零
unshare -Ur ./captest4                            # §2  userns 无效

# 2) 端到端非特权运行（无需 root）
#    把 check_root() 改成只告警后构建
cargo build --release && ./np-tier3.sh            # §1  310/310、0 回退、0 归因

# 2b) group 兼容性矩阵（无需 root）—— §5.2
gcc -O2 -o fsidmatrix fsidmatrix.c && ./fsidmatrix
gcc -O2 -o fsidmix    fsidmix.c    && ./fsidmix

# 3) 内核依据
curl -s https://raw.githubusercontent.com/torvalds/linux/v6.12/fs/notify/fanotify/fanotify_user.c \
  | sed -n '670,700p;1176,1230p;1411,1426p;1490,1500p;1565,1605p;1765,1780p'
curl -s https://raw.githubusercontent.com/torvalds/linux/v6.12/include/linux/fanotify.h \
  | sed -n '30,60p'                               # §3  flag 集合对比

# 4) systemd 白名单审计（§5.5 / 阶段 2）
systemd-analyze syscall-filter @system-service | grep -c fanotify   # -> 0
for s in mount setns unshare bpf init_module open_by_handle_at; do
  systemd-analyze syscall-filter @system-service | grep -cx "    $s"
done                                                                 # -> 全 0

# 5) 支点假设验证（需 root 一次）—— §8
cd ~/.projects/fsmon/tools
gcc -O2 -o verify-fanotify-privsep verify-fanotify-privsep.c
sudo ./verify-fanotify-privsep 1000            # 期望 CONFIRMED

# 6) 每 uid 的 fanotify 硬上限（§5.7）
cat /proc/sys/fs/fanotify/max_user_marks     # 本机 143129
cat /proc/sys/fs/fanotify/max_user_groups    # 本机 128
```
